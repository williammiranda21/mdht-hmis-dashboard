node_modules/
.next/
out/
.env.local
.env*.local
# incremental TypeScript build cache — machine-specific, regenerated on build
tsconfig.tsbuildinfo
.DS_Store
*.log
pipeline/__pycache__/
# Temp login credentials written by seed_users.py — never commit
pipeline/.seeded_credentials.txt

# Claude Code personal settings + impeccable's per-machine engine binary
.claude/settings.local.json
.claude/skills/impeccable/scripts/bin/
.github/skills/impeccable/scripts/bin/
